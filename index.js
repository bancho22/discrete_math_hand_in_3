import {
    membership,
    intersection,
    union,
    difference,
    complement,
    compareSets
} from './setAPI'

let res = compareSets([1, 2, 3], [1, 2, 3])

console.log(res)

// res.forEach(e => console.log(e))
